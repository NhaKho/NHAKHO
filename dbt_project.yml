name: ecommerce_lakehouse
version: '1.0'
config-version: 2

profile: ecommerce_lakehouse

model-paths: ["models"]

models:
  ecommerce_lakehouse:
    silver:
      +materialized: table
      +schema: silver
    gold:
      +materialized: table
      +schema: gold